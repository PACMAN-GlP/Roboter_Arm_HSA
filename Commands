connect to pi console(in pc-terminal): →ssh {pi-name}@{ip-adress}

transfer file to pi(in pc terminal): →scp {file directory on pc e.g. /path/to/file/Main.py} {pi-name}@{ip-adress}:~{target file directory on pi. default(leave blanc): /home/*pi's name*/}

start python program(in pi): →python3 {/path/to/file/Main.py}

adding files to push to github repository(preparing the upload): →git add {/path/to/file/Main.py}

commit files to github repository with comment: →git commit -m "{push description}"

push files to github repository: →git push